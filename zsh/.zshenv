# =============================================================================
# ~/.zshenv — Sourced for all shell invocations (login, interactive, scripts)
# Keep this minimal — only things every shell instance needs
# =============================================================================

# Cargo / Rust
. "$HOME/.cargo/env"
